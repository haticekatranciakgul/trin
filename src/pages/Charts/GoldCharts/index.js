import React from 'react';
import MainLayout from '../../../layouts/MainLayout';
import { Typography } from '@mui/material';

const GoldCharts = () => {
  return (
    <MainLayout>
      <Typography variant="h4" sx={{ mt: 4 }}>
        Gold Charts Sayfası (içerik yakında)
      </Typography>
    </MainLayout>
  );
};

export default GoldCharts; 